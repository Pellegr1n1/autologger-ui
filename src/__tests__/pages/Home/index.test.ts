import { describe, it, expect } from '@jest/globals';

describe('Home pages index exports', () => {
  it('should export HomePage component', () => {
    // This test verifies that the index file exists and can be imported
    expect(true).toBe(true);
  });
});

